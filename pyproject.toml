[project]
name = "geographic-databases"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fiona>=1.10.1",
]

[tool.uv.workspace]
members = [
    "docs/concepts",
]
